<?php

///////////////////////////////////////////
// Основной скрипт инициализации проекта //
///////////////////////////////////////////
include(get_template_directory() . '/lib/init.php');

///////////////////////////////////////
// Подключение фукциональных файлов  //
///////////////////////////////////////
foreach(glob(get_template_directory() . '/lib/func.d/*.php') as $file) {
	include_once $file;
}

//////////////////////////////////////
// Подключение файлов ajax запросов //
//////////////////////////////////////
foreach(glob(get_template_directory() . '/lib/ajax.d/*.php') as $file) {
	include_once $file;
}

////////////////////////////////////////////////////////
// Подключение файлов отвечающих за страници настроек //
////////////////////////////////////////////////////////
foreach(glob(get_template_directory() . '/lib/opt.d/*.php') as $file) {
	include_once $file;
}